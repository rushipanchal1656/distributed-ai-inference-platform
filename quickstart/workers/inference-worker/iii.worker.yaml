
name: inference-worker

runtime:
  kind: python
  package_manager: pip
  entry: inference_worker.py

scripts:
  install: "pip install -r requirements.txt"
  start: "python inference_worker.py"

sandbox:
  enabled: false
